{
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": "php artisan config:clear && php artisan route:clear && php artisan migrate --force",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
